# Backups directory
This directory stores system backups created by the backup module.

Backup types include:
- System configuration backups
- Database backups
- Website backups
- SSL certificate backups
- Complete system snapshots

Backup files are excluded from git commits due to size and sensitivity.
