# Configuration directory
This directory stores system configuration files and templates.

Contains:
- Service configuration templates
- Custom configuration files
- User-specific settings
- Module configurations

Sensitive configuration files are excluded from git commits.
